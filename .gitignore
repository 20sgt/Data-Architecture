# raw landing zone (bronze) — never commit scraped data
raw/

# warehouse local artifacts
warehouse/db/
warehouse/exports/

# dev env / scratch
.env
.env.*
!.env.example
!**/.env.example
.venv/
.probe/
logs/
*.log

# python
__pycache__/
*.pyc
.pytest_cache/

# os
.DS_Store

# slop
.claude

# dbt: throwaway build output, downloaded packages, logs, and the REAL
# connection file (holds a secret token — commit only profiles.yml.example)
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/profiles.yml
dbt/.env
dbt/.user.yml

# Terraform local artifacts (state lives in gs://cotc-tfstate; plans are throwaway)
terraform/.terraform/
terraform/tfplan*
*.tfstate
*.tfstate.*

# ---------------------------------------------------------------------------
# Podcast pipeline (sfchronicle_podcast_ingest)
# Do NOT use a blanket *.json rule — main tracks sample/**/*.json fixtures.
# ---------------------------------------------------------------------------
sfchronicle_podcast_ingest/.venv/
sfchronicle_podcast_ingest/logs/
sfchronicle_podcast_ingest/data/*.sqlite
sfchronicle_podcast_ingest/data/*.sqlite-*
sfchronicle_podcast_ingest/**/__pycache__/
sfchronicle_podcast_ingest/.pytest_cache/

# GCP / local secret JSON keys (never commit)
**/corn-off-the-cobb*.json
**/*service-account*.json
**/*credentials*.json
**/*-bdc8978dd3c6.json
